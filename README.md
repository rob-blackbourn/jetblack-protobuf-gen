# protobuf-ex1

A pythonic wrapper around the classes generated by the standard protoc generator.

## Usage

```bash
protoc \
    --proto_path=$PROTOBUF_HOME/include \
    --proto_path=./protos \
    --python_out=src/jetblack_protobuf_gen/pb \
    --pyi_out=src/jetblack_protobuf_gen/pb \
    --jetpy_out=src/jetblack_protobuf_gen/pb \
    ./protos/*.proto
```

## Things to do

The following needs attention:

* Any
* Enums
* Maps
* Groups
* Structs
* __init__ files
